<nav>
    <ul>
        <li><a href="{{ path('main_home') }}" title="Home">Accueil</a></li>
        {% if app.user %}
            <li><a href="{{ path('app_logout') }}" title="register">Se déconnecter</a></li>
        {% else %}
            <li><a href="{{ path('app_login') }}" title="login">Se connecter</a></li>
            <li><a href="{{ path('app_register') }}" title="register">S'enregistrer</a></li>
        {% endif %}
    </ul>
</nav>